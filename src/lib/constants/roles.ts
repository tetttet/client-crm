import type { EmployeeRole } from "@/lib/api/types/employee.types";

export const EMPLOYEE_ROLES: EmployeeRole[] = ["admin", "manager", "user"];

export const MANAGERIAL_EMPLOYEE_ROLES: EmployeeRole[] = ["admin", "manager"];
